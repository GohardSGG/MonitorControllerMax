# 日志核查与点击链路诊断（本轮）

- 时间戳: 2026-02-26 19:37:44 +08:00
- 目录: `C:\Code\Music\MonitorControllerMax`

## 你提的问题
- 为什么不先核查日志。

## 本轮已执行的真实日志核查

1. 先确认日志真实路径
- 代码里 `InstanceLogger::get_log_path()` 在 Windows 使用：
  - `C:\Plugins\MCM_Logs`
- 不是 `Doc/Log`。

2. 读取最新日志实例
- 最新文件：
  - `C:\Plugins\MCM_Logs\MCM_7dc0d2fc_2026-02-26_19-29-19.log`
- 关键证据：
  - 有 `SOLO` 状态机日志：`[interaction] [SM] SOLO: (None,None)->(Solo,None)`
  - 但没有任何 `"[CH] ... click"` 日志
- 结论：
  - `SOLO` 按钮事件已进入 Rust。
  - 通道点击没有进入 `InteractionManager::on_channel_click`（至少未到该层）。

## 本轮新增“强诊断”修复

为精确抓到断点，我已在 Slint 回调入口加完整日志：
- 主通道点击入口 `app.on_channel_click`
- SUB 点击入口 `app.on_sub_channel_click`
- SUB 右键入口 `app.on_sub_channel_secondary_click`

每个入口都会记录：
- 收到的 UI 索引
- role / automation 状态
- 是否被 guard 拦截
- 索引是否匹配 layout 通道
- 是否真正应用状态变化

文件：
- `Source/Plugin/src/editor/slint_host.rs`

## 编译验证

```powershell
cargo check -p monitor_controller_max
```

通过。

## 下一步（等你复现一次）
- 你重新加载插件后按这个顺序操作：
  1) 点 `SOLO`
  2) 点 `L` 或 `R`
- 我再读取 `C:\Plugins\MCM_Logs` 最新日志，就能精确定位是：
  - UI 命中没触发回调，还是
  - 回调触发但被 guard/索引映射挡住。
