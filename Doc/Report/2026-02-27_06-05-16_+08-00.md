# MonitorControllerMax Resize 深度对照修复报告（IRMax逐行比对）

- 时间: 2026-02-27 06:05:16.395 +08:00
- 对照目标: `C:\Code\Music\IRMax`
- 当前项目: `C:\Code\Music\MonitorControllerMax`
- 结论: 已发现并修复关键缺口，不是核心依赖版本不一致导致。

## 逐行对照发现的关键差异

### IRMax 正常路径（已验证）
1. `host_resized()` 不只更新 coordinator，还会把尺寸投递回 UI：
   - `C:\Code\Music\IRMax\Source\IRMax_Plugin\src\editor\slint_host\bridge.rs:152`
   - `C:\Code\Music\IRMax\Source\IRMax_Plugin\src\editor\slint_host\bridge.rs:161`
2. UI 线程消费该消息并执行 `window.set_size(...)`：
   - `C:\Code\Music\IRMax\Source\IRMax_Plugin\src\editor\slint_host\runtime.rs:723`
   - `C:\Code\Music\IRMax\Source\IRMax_Plugin\src\editor\slint_host\runtime.rs:745`

### MonitorControllerMax 原问题
- 旧实现只有 `on_host_resized()`，没有“回写 UI window size”链路，导致 resize 握手不完整。

## 本轮已实施修复（对齐 IRMax）

1. 增加 UI 消息桥接（`SetWindowSize`）
- 新增 `UiMessage` / `SlintBridgeState`
- 文件: `Source/Plugin/src/editor/slint_host.rs:32`
- 文件: `Source/Plugin/src/editor/slint_host.rs:36`

2. 在 `spawn()` 建立 `mpsc` 通道并把 receiver 注入 `SlintHostView`
- 文件: `Source/Plugin/src/editor/slint_host.rs:132`
- 文件: `Source/Plugin/src/editor/slint_host.rs:155`

3. 在 `host_resized()` 中对齐 IRMax：
- `resize_coordinator.on_host_resized(...)`
- 发送 `SetWindowSize(constrained_w, constrained_h)` 到 UI
- 文件: `Source/Plugin/src/editor/slint_host.rs:194`
- 文件: `Source/Plugin/src/editor/slint_host.rs:202`

4. 在 `SlintHostView` 增加消息消费并调用 `window.set_size(...)`
- `flush_ui_messages()` + `on_event()` 中执行
- 文件: `Source/Plugin/src/editor/slint_host.rs:1047`
- 文件: `Source/Plugin/src/editor/slint_host.rs:1053`
- 文件: `Source/Plugin/src/editor/slint_host.rs:1065`

5. 新增 resize 诊断日志
- UI请求日志：`UI requested resize ...`
- Host回调日志：`Host resized ...`
- 便于下份日志直接判断卡在 UI 还是 Host

## 依赖库排查结论（你要求的 Library 深查）

对比目录：
- `Library/nih-plug-slint`
- `Library/plugin-things/plugin-canvas`
- `Library/plugin-things/plugin-canvas-slint`

结果：
- 关键文件哈希一致（SHA256）：
  - `Library/nih-plug-slint/src/resize.rs` 与 IRMax 一致
  - `Library/nih-plug-slint/Cargo.toml` 与 IRMax 一致
  - `Library/plugin-things/plugin-canvas/Cargo.toml` 与 IRMax 一致
  - `Library/plugin-things/plugin-canvas-slint/Cargo.toml` 与 IRMax 一致
- 结论：**此次 resize 问题不是这几项核心依赖版本差异导致，主因是项目层桥接链路缺失。**

## 编译与部署
- 已通过：
  - `cargo check -p monitor_controller_max --features gui-slint`
  - `cargo check`
  - `cargo xtask bundle monitor_controller_max --dev`
- 已部署：
  - `C:\Plugins\VST Dev\monitor_controller_max.vst3\Contents\x86_64-win\monitor_controller_max.vst3`
  - 时间戳: 2026-02-27 06:04:05

## 建议你立即验证
1. 关闭并重开宿主后，拖动右下角 resize 手柄。
2. 把最新日志给我（`C:\Plugins\MCM_Logs` 下最新 `.log`），我会直接看新增的 `[slint-resize]` 两类日志：
   - `UI requested resize ...`
   - `Host resized ...`
