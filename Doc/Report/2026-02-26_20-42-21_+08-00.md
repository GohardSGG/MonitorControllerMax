# MonitorControllerMax 前端深度排查与修复报告

- 时间: 2026-02-26 20:42:57.183 +08:00
- 项目: `C:\Code\Music\MonitorControllerMax`
- 目标: 恢复通道按钮点击（L/R/C...）、Solo/Mute联动、并确保新前端架构稳定不丢事件

## 核心结论
1. 发现通道点击失效的关键风险点：UI 每 50ms 在 `refresh_runtime_ui()` 里 `set_channels(...)` 重建整组通道组件，`TouchArea.clicked` 在按下/抬起期间容易被重建中断，导致点击事件丢失。
2. 发现运行版本混淆风险：用户提供日志中没有新加的 `slint-click` 诊断行，说明宿主可能未加载最新构建；已重新打包并部署到 `C:\Plugins\VST Dev`。

## 本轮代码修复
### 1) 通道模型增量更新（避免高频重建吞点击）
- 文件: `Source/Plugin/src/editor/slint_host.rs`
- 关键改动:
  - 新增 `ChannelUiSnapshot`，缓存上一帧通道UI快照。
  - `PollState` 新增 `prev_grid_width/prev_grid_height/prev_channels`。
  - 仅当快照发生变化时才 `app.set_channels(...)`。
  - 网格尺寸同样改为“变化时更新”，无布局时也做条件回退。
- 参考位置:
  - `Source/Plugin/src/editor/slint_host.rs:32`
  - `Source/Plugin/src/editor/slint_host.rs:60`
  - `Source/Plugin/src/editor/slint_host.rs:1204`
  - `Source/Plugin/src/editor/slint_host.rs:1264`

### 2) Speaker 点击触发改为 pointer down 即时触发
- 文件: `Source/Plugin/ui/pages/home_page.slint`
- 关键改动:
  - `SpeakerBox` 增加 `enabled` 属性。
  - 将点击触发从 `clicked` 改为 `pointer-event(down + left)`，降低刷新期间丢点击概率。
  - 通道交互绑定 `root.channel-interaction-enabled`。
- 参考位置:
  - `Source/Plugin/ui/pages/home_page.slint:154`
  - `Source/Plugin/ui/pages/home_page.slint:161`
  - `Source/Plugin/ui/pages/home_page.slint:189`
  - `Source/Plugin/ui/pages/home_page.slint:760`

## 构建与部署
- 已执行:
  - `cargo check -p monitor_controller_max --features gui-slint`（通过）
  - `cargo check`（通过）
  - `cargo xtask bundle monitor_controller_max --dev`（通过，已自动部署）
- 当前 Dev 插件文件:
  - `C:\Plugins\VST Dev\monitor_controller_max.vst3\Contents\x86_64-win\monitor_controller_max.vst3`
  - 时间戳: 2026-02-26 20:39:42（本轮最新）

## 系统目录部署状态
- 尝试覆盖 `C:\Program Files\Common Files\VST3\monitor_controller_max.vst3` 时被拒绝（`Access is denied`），该位置旧文件时间仍为 2026-01-28。
- 这通常意味着系统目录插件被宿主占用或权限不足。

## 影响说明
- 该修复针对“通道按钮无反应”的底层事件链路进行：
  - 消除高频重建导致的点击丢失
  - 提前在 pointer down 触发通道事件
- Solo/Mute 按钮逻辑保留并继续同步参数/OSC状态。
